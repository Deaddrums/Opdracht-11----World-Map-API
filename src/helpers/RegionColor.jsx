import './RegionColor.css'

export function regionColor (region) {
if (region === "Europe") return "yellow"
    if (region === "Africa") return "blue"
    if (region === "Asia") return "red"
    if (region === "Oceania") return "purple"
    if (region === "Americas") return "green"
}

